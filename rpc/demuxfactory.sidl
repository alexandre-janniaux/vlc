namespace vlc
{
    struct RemoteAccess
    {
        u64 port;
        u64 object_id;
    }

    struct RemoteEsOut
    {
        u64 port;
        u64 object_id;
    }

    interface DemuxFactory
    {
        create(RemoteAccess access, RemoteEsOut out, string module, bool preparsing) -> (u64 demux_object);
    }
}
